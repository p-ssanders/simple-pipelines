---
platform: linux
params:
  RDS_ADDRESS:
  RDS_PORT:
  RDS_USERNAME:
  RDS_PASSWORD:
run:
  path: sh
  args:
  - "-c"
  - |

    mysql -h "${RDS_ADDRESS}" \
    -P "${RDS_PORT}" \
    -u "${RDS_USERNAME}" \
    -p "${RDS_PASSWORD}" \
    -e "create database bosh;"
